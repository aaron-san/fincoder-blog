{
  "homepage": "https://aaron-san.github.io/fincoder-blog",
  "name": "fincoder-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && next build && next export",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && next start",
    "lint": "set NODE_OPTIONS=--openssl-legacy-provider && next lint"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^4.2.12",
    "mathjax": "^3.2.2",
    "next": "^11.1.4",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-katex": "^3.0.1"
  },
  "devDependencies": {
    "eslint": "7.29.0",
    "eslint-config-next": "11.0.1"
  }
}
